# Sonder survey swarm (Stage 2).
# Mock mode (no LLM) needs only the service deps below.

# HTTP service the frontend calls
fastapi>=0.110
uvicorn[standard]>=0.29

# Real-agent mode (set ANTHROPIC_API_KEY)
anthropic>=0.40
