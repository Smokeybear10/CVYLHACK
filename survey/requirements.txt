# Survey swarm (Stage 2) dependencies. Install into the Python 3.11+ venv:
#   .venv/bin/pip install -r survey/requirements.txt
pydantic>=2.0
anthropic>=0.40        # real agent; not needed for the stub/offline path
reportlab>=4.0         # PDF generation (pure Python, no system libs)
pypdf>=4.0             # PDF content assertions in tests
